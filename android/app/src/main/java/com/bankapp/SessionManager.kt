package com.bankapp

class SessionManager {
  // Esto está vacío a propósito. Acá iría el cifrado con Keystore.
  fun clearSession() {
    // Acá borraría la sesión cifrada cuando expira o cuando el usuario cierra sesión.
  }
}